# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'peakviewerar' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for peakviewerar
  pod 'ARCL'

  target 'peakviewerarTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'peakviewerarUITests' do
    # Pods for testing
  end

end
